# Walkthrough - Final Health Connect Manifest Cleanup

I have completed the exhaustive cleanup of the Android Manifest to resolve the recurring "Excessive data access" issue from Google Play.

## Changes Made

### 1. Exhaustive Manifest Cleanup
- **[AndroidManifest.xml](file:///C:/Users/amayj/ironlog/ironlog/new_app/android/app/src/main/AndroidManifest.xml)**:
    - Rewrote the Health Connect section to be completely unambiguous.
    - Explicitly removed **every single permission** that the `@capgo/capacitor-health` library adds by default, including `WRITE_TOTAL_CALORIES_BURNED`, `WRITE_ACTIVE_CALORIES_BURNED`, `READ_STEPS`, `READ_HEART_RATE`, etc.
    - Verified the **Merged Manifest** in the build folder to confirm that only the 5 required permissions remain in the final application binary.

### 2. Version Bump
- **[build.gradle](file:///C:/Users/amayj/ironlog/ironlog/new_app/android/app/build.gradle)**:
    - Updated `versionCode` to **134** and `versionName` to **1.0.3**. This ensures Google Play processes it as a new version with the updated manifest.

## Verification Results

### Merged Manifest Check
I inspected the final merged manifest generated by the Android build system. It confirms that the app now contains **ONLY** the following Health Connect permissions:
1. `READ_ACTIVE_CALORIES_BURNED`
2. `READ_TOTAL_CALORIES_BURNED`
3. `READ_WEIGHT`
4. `WRITE_WEIGHT`
5. `READ_EXERCISE`

## Next Steps for Resubmission

1. **Re-build App Bundle**: Run `.\gradlew :app:bundleRelease` in your terminal.
2. **Upload to Play Console**: Upload the new `.aab` file (Version 134).
3. **Verify Declaration**: Ensure your Health Apps declaration in the Play Console matches these 5 permissions.

> [!IMPORTANT]
> The manifest is now as clean as possible. As long as your in-console declaration matches these 5 permissions, this submission should pass the "Minimum Scope" review.
